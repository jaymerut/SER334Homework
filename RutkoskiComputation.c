/**
* (The program takes the height and radius of a cylinder that the user provides
* and calculates the volume and displays it back to the user. The program can
* do as many calculations as the user wants.)
*
* Completion time: (15 minutes)
*
* @author (Jayme Rutkoski)
* @version (10/12/2018)
*/

#include <stdio.h>

/*int main(void){
    int num;
    float height, radius, result;
    printf("How many cylinders would you like to sum together?\n");
    scanf("%d", &num);
	//Add a prompt for the number of cylinders to sum. Make sure to use the control
	//symbol for integers. [2 points]
    while(num != 0){
        printf("Height of cylinder: ");
        scanf("%f", &height);
        printf("Radius of cylinder: ");
        scanf("%f", &radius);
        result = 3.14 * radius * radius * height;
        printf("The volume of the cylinder is: %f", result);
        num--;
    }
	return 0;
}*/